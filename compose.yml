services:
  postgres:
    image: postgres:16-alpine
    networks:
      db:
        aliases:
          - makepad-postgres
      le_petit_coin:
        aliases:
          - makepad-postgres-le-petit-coin

networks:
  db:
  le_petit_coin:
