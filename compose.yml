services:
  postgres:
    image: postgres:16-alpine
    networks:
      db:
        aliases:
          - makepad-postgres
      le_petit_coin:
        aliases:
          - makepad-postgres-le-petit-coin
      vif:
        aliases:
          - makepad-postgres-vif

networks:
  db:
  le_petit_coin:
  vif:
