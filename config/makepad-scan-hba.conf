# Insert before shared catch-all rules. The application connects over WireGuard.
hostnossl makepad_scan all all reject
hostssl makepad_scan makepad_scan_app 10.80.0.1/32 scram-sha-256
hostssl makepad_scan makepad_scan_app 127.0.0.1/32 scram-sha-256
host makepad_scan all all reject
host all makepad_scan_app all reject
